class Empt(object):
    def e(self):
        return

class Fib(object):
    def __init__(self, fst  , snd) -> None:
        super().__init__()
        self.fst = fst
        self.snd = snd

    def foo(self, fooArg):
        fooVar = 3
        return fooVar
def identity(z,z1):
        return z

y = Fib(1,2)
y.foo = Empt
y.foo()
identity = Fib

x = identity(1, y)